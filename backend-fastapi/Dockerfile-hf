# Use a slim Python base image
FROM python:3.9-slim

# Set the working directory in the container
WORKDIR /app

# Copy the requirements file and install dependencies
COPY requirements-hf.txt .
RUN pip install --no-cache-dir -r requirements-hf.txt

# Copy the application code and data
# We'll copy the production main file as app.py
COPY main_production.py app.py
# Copy the embeddings from the public folder
COPY ../public/workspace_embeddings.json .

# Expose the port the app runs on
EXPOSE 7860

# Command to run the app
# Use 0.0.0.0 to be accessible from outside the container
# The port is set to 7860, a common default for HF Spaces
CMD ["uvicorn", "app:app", "--host", "0.0.0.0", "--port", "7860"]
